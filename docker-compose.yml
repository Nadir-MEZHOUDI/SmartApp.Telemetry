services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: telemetry
      POSTGRES_USER: telemetry
      POSTGRES_PASSWORD: change-this-password
    volumes:
      - telemetry-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U telemetry -d telemetry"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    environment:
      ConnectionStrings__Telemetry: Host=postgres;Port=5432;Database=telemetry;Username=telemetry;Password=change-this-password
      Dashboard__AdminKey: change-this-admin-key
      Dashboard__AllowedOrigin: http://localhost:8080
    ports:
      - "5000:8080"
    depends_on:
      postgres:
        condition: service_healthy

  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    environment:
      TelemetryApi__BaseUrl: http://localhost:5000
      TelemetryApi__AdminKey: change-this-admin-key
    ports:
      - "8080:8080"
    depends_on:
      - api

volumes:
  telemetry-postgres:
